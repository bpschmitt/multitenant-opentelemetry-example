{{- if .Values.receiver.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.receiver.name }}
  labels:
    app: {{ .Values.receiver.name }}
    tenant: {{ .Values.global.tenantId }}
    component: receiver
spec:
  type: {{ .Values.receiver.service.type }}
  ports:
    - port: {{ .Values.receiver.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.receiver.name }}
{{- end }}

